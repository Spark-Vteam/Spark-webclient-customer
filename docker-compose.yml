version: "3"
services:
    webclient-customer:
        build:
            context: .
            dockerfile: Dockerfile
        restart: always
        ports:
            - 3000:3000
        expose:
            - '3000'
        stdin_open: true